# Resolvera Environment Variables
# Copy this file to .env and configure your secrets
#
# Quick Setup for Docker:
#   1. Copy: cp .env.example .env
#   2. Generate secrets (see commands below)
#   3. Update POSTGRES_PASSWORD, JWT_SECRET, ENCRYPTION_KEY
#   4. Run: docker compose up -d

# ====================================
# Database Configuration (Docker)
# ====================================
POSTGRES_USER=resolvera
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_PASSWORD
POSTGRES_DB=resolvera

# ====================================
# Security (REQUIRED - Must Generate!)
# ====================================
# Generate JWT_SECRET with: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_JWT_SECRET_MINIMUM_32_CHARACTERS

# Generate ENCRYPTION_KEY with: openssl rand -hex 16  (must be exactly 32 chars)
ENCRYPTION_KEY=CHANGE_THIS_32_CHARACTER_KEY_

# Generate PASSWORD_HASH_SECRET with: openssl rand -hex 16  (must be exactly 32 chars)
PASSWORD_HASH_SECRET=CHANGE_THIS_32_CHARACTER_KEY_

# Generate ZONE_API_HASH_SECRET with: openssl rand -hex 16  (must be exactly 32 chars)
ZONE_API_HASH_SECRET=CHANGE_THIS_32_CHARACTER_KEY_

# ====================================
# Application Settings
# ====================================
# Application port (default: 3000)
HOST_PORT=3000


# Node environment
NODE_ENV=production

# Disable Next.js telemetry
NEXT_TELEMETRY_DISABLED=1
